# Development, testing and type-checking dependencies
-r requirements.txt

# Testing
hypothesis>=6.90.0
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
moto>=4.2.0  # For mocking AWS services

# Type checking
mypy>=1.5.0
boto3-stubs[dynamodb,logs]>=1.28.0

# Linting / formatting (pinned so CI and local runs agree on style)
black==26.1.0
ruff==0.14.14
